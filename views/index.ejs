<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>

<body class="bg-zinc-800 text-white">
    <div class="main w-full ">
        <div
            class="form p-10 text-white border-2  border-zinc-700 rounded-md w-1/3 mx-auto mt-10 shadow-xl shadow-black/40">
            <form action="/create" method="post">
                <input type="text" class="block w-full rounded-md px-5 outline-none py-2 bg-zinc-600" name="title"
                    placeholder="Enter text here">
                <textarea class="block mt-2 w-full  rounded-md outline-none px-5 py-2 bg-zinc-600"
                    placeholder="Enter more text here" name="details"></textarea>
                <input type="submit" class="mt-2 bg-blue-600 active:bg-blue-700 rounded p-2 font-bold"
                    value="Create task">

            </form>
        </div>
        <div class="tasks p-10 flex gap-4">
            <% if ( files.length> 0) { %>
                <% files.forEach((val)=> { %>
                    <div class="task bg-zinc-600 w-100  px-3 py-4 rounded-md mb-4 shadow-xl shadow-black/40">
                        <h2 class="text-lg font-bold">
                            <%= val %>
                        </h2>

                        <a class="text-blue-500 underline inline-block mt-3" href="/file/<%= val %>">Readme .</a>
                        <div class="flex">
                            <a class="text-zinc-400 underline" href="/edit/<%= val %>">Edit filename</a>
                        </div>
                        <form action="/delete" method="POST" class="mt-2">
                            <input type="hidden" name="filesname" value='<%= val %>'>
                            <button type="submit"
                                class="bg-red-600 px-3 py-1 rounded active:bg-red-900 text-white">Delete</button>
                        </form>



                    </div>
                    <% }) %>

                        <% } else { %>
                            <h3 class="bg-zinc-600">No tasks yet</h3>
                            <% } %>


        </div>
    </div>
</body>

</html>